import { createRoot } from 'react-dom/client';
import { App } from './App';
import './index.css';

/**
 * This is the manual testing app for the @jtjs/react lib.
 */

createRoot(document.getElementById('root')!).render(<App />);
